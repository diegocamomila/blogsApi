// const { BlogPost } = require('../database/models');
// const getPost = require('./getPost');

// const update = async (id, { title, content }) => {
//   const properties = { title, content, updated: formatDate() };
//   const where = { id };
//   await BlogPost.update(properties, { where });
// };

// // const putIdServece = async (postData, id) => {
// //   await BlogPost.update(postData, { where: { id } });
// //   const post = getPost(id);
  
// //   return post;
// // };
 
//   module.exports = putIdServece;
